# ─── Core Web Framework ─────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.9,<1.0.0

# ─── Database (SQLAlchemy + SQLite default) ──────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0

# ─── Configuration & Validation ─────────────────────────────────────────────
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# ─── Numeric / Vector Math (used by local embeddings) ───────────────────────
numpy>=1.26.0,<3.0.0

# ─── HTTP Client (used by AI integrations) ──────────────────────────────────
httpx>=0.27.0,<1.0.0

# ─── Document Processing ─────────────────────────────────────────────────────
PyMuPDF>=1.24.0,<2.0.0

# ─── AI Provider Integrations (optional — only needed if AI_PROVIDER != "deterministic") ──
# Uncomment if using OpenAI or Anthropic:
# langchain>=0.2.0,<0.4.0
# langchain-openai>=0.1.0,<0.4.0
# langchain-anthropic>=0.1.0,<0.4.0

# ─── PostgreSQL + pgvector (optional — only needed if using PostgreSQL) ───────
# Uncomment if using PostgreSQL as DATABASE_URL:
# psycopg2-binary>=2.9.0,<3.0.0
# pgvector>=0.3.0,<1.0.0

# ─── Resilience ──────────────────────────────────────────────────────────────
tenacity>=8.3.0,<10.0.0

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
